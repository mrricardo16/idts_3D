# MVP-10A-05：回退、性能与生命周期

> 状态：Blocked。依赖 MVP-10A-04。

目标：验证 Tiles 开关、GLB-only fallback、请求取消、销毁、性能和资源生命周期。禁止伪造性能数据、放宽 POC 基线或删除可观察错误状态。

验收：失败注入、重复进出、冷暖缓存、纯 GLB 对照、资源释放均有证据。回滚：关闭 TilesLayer 和新入口，恢复 GLB-only 路径。Codex 提示词：按性能预算和结果模板记录证据，先输出验证计划并等待授权。
